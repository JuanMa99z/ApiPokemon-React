// helper creado para devolver la primera letra en mayuscula, helper que se usa en el PokemonPage.jsx
export const primerMayuscula = (word) => {
  return word[0].toUpperCase() + word.substring(1);
};
